spring:
  profiles:
    active: local
    include: my
  jpa:
    hibernate:
#       ddl-auto: create
    properties:
      hibernate:
        default_batch_fetch_size: 100
        # show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug